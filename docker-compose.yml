version: "3.9"

services:
  dashboard:
    build: .
    container_name: dashboard_terremotos
    ports:
      - "8501:8501"
    environment:
      - DB_HOST=host.docker.internal
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=
      - DB_NAME=terremotos_db